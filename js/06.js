// Objeto Math
let resultado

resultado = Math.PI;
resultado = Math.round(2.6); // Redondeo no tan estricto
resultado = Math.ceil(2.1); // ceil siempre redondea hacia arriba
resultado = Math.floor(2.1); // floor siempre redondea hacia abajo
resultado = Math.sqrt(144); // raiz cuadrada
resultado = Math.abs(-200); // valor absoluto
resultado = Math.min(1, 5, 100, 200 , 10, 37); // el menor numero
resultado = Math.max(1, 5, 100, 200 , 10, 37); // el mayor numero
resultado = Math.floor(Math.random() * 30);

console.log(resultado);