// Diferencia entre método y función

const numero1 = 20;
const numero2 = '20';

// Funciones tienen el nombre de la función seguido de paréntesis
console.log(parseInt(numero2)); // parseInt es una función

// Los métodos usualmente tienen el nombre de la variable o del objeto seguidop de un punto
console.log(numero1.toString()); // .toString es un método